import os
from sammy.repositories.base_repository import BaseRepository

USERS_TABLE = os.environ['USERS_TABLE_NAME']

class UsersRepository(BaseRepository):

    def __init__(self):
        super().__init__(USERS_TABLE, 'id', 'username')

